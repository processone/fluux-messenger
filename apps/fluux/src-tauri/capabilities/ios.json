{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "ios",
  "description": "iOS shell with native XMPP TCP/TLS proxy",
  "platforms": ["iOS"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "os:default",
    "opener:default"
  ]
}
